apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true

alerting:
  alertmanagers:
    - type: prometheus
      host: alertmanager:9093